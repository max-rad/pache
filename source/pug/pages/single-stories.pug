extends ../components/base/layout

block variables
    - const title = "Паче — Истории — Камерная новогодняя встреча"
    - const wrapperMods = ""
    - const hasHeader = true
    - const headerMods = ""
    - const hasFooter = true
    - const footerMods = ""
    - const currentUrl = "single-stories.html"

block main
    main
      +single-stories()
      +look-in-history()


block modals
  +modal("modal--add-to-basket")(data-modal="add-to-basket")
    +add-to-basket()
  +modal("modal--complete-look")(data-modal="complete-look")
    +complete-look-modal()
  +modal("modal--basket")(data-modal="basket")
    +basket()
